<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gx.dao.BookingcommissionDao" >
  <resultMap id="BaseResultMap" type="com.gx.po.BookingcommissionPo" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Sun Oct 16 19:18:30 CST 2016.
    -->
    <constructor >
      <idArg column="id" jdbcType="INTEGER" javaType="java.lang.Integer" />
      <arg column="name" jdbcType="VARCHAR" javaType="java.lang.String" />
      <arg column="phoneName" jdbcType="VARCHAR" javaType="java.lang.String" />
      <arg column="money" jdbcType="DOUBLE" javaType="java.lang.Double" />
      <arg column="supplierId" jdbcType="INTEGER" javaType="java.lang.Integer" />
      <arg column="roomId" jdbcType="INTEGER" javaType="java.lang.Integer" />
      <arg column="time" jdbcType="VARCHAR" javaType="java.lang.Integer" />
      <arg column="createTime" jdbcType="TIMESTAMP" javaType="java.sql.Timestamp" />
    </constructor>
  </resultMap>


  <insert id="inserAll" parameterType="com.gx.po.BookingcommissionPo" useGeneratedKeys="true"  keyProperty="id">
      INSERT INTO `bookingcommission` ( `name`,phoneName,`money`, `supplierId`,`roomId`,`time`)
     VALUES ( #{name,jdbcType=VARCHAR}, #{phoneName,jdbcType=VARCHAR},#{money,jdbcType=DOUBLE},#{supplierId,jdbcType=INTEGER},
            #{roomId,jdbcType=INTEGER}, #{time,jdbcType=VARCHAR} ) ;
</insert>

  <select id="list" resultType="com.gx.po.BookingcommissionPo">
    SELECT * FROM `bookingcommission` WHERE `name` LIKE CONCAT('%',#{name,jdbcType=VARCHAR},'%')
  </select>

  <select id="selectMoney" resultType="com.gx.po.BookingcommissionPo">
SELECT *,SUM(`money`)AS booking FROM `bookingcommission` WHERE DATE_FORMAT(`time`, '%Y-%m')= #{time,jdbcType=VARCHAR}
  </select>
  <select id="select" resultType="com.gx.po.BookingcommissionPo" parameterType="com.gx.po.BookingcommissionPo">
    SELECT *
     FROM `bookingcommission` WHERE `name`=#{name,jdbcType=VARCHAR}
     AND `phoneName`= #{phoneName,jdbcType=VARCHAR} AND  `roomId`=#{roomId,jdbcType=INTEGER}
     AND`supplierId`=#{supplierId,jdbcType=INTEGER}
      AND  DATE_FORMAT(`time`, 'yyyy-MM-dd')=DATE_FORMAT( #{time,jdbcType=VARCHAR}, 'yyyy-MM-dd')
    </select>

  <update id="updateTimeRoom" parameterType="com.gx.po.BookingcommissionPo">
    UPDATE `bookingcommission`
    SET
      `name` =#{name,jdbcType=VARCHAR},
      `phoneName` = #{phoneName,jdbcType=VARCHAR},
      `money` = #{money,jdbcType=DOUBLE},
      `supplierId` =#{supplierId,jdbcType=INTEGER},
      `roomId` =`roomId`=#{roomId,jdbcType=INTEGER},
      `time` =#{time,jdbcType=VARCHAR}
    WHERE `id` =#{id,jdbcType=INTEGER};
  </update>

  <select id="selectAll"  resultType="com.gx.po.BookingcommissionPo">
    SELECT b.*,s.`supplierName`,r.`id` AS roomId,r.`roomNumber`
	FROM `bookingcommission` b INNER JOIN `room` r ON b.`roomId`=r.`id`
	INNER JOIN `supplier` s ON b.`supplierId`=s.id
	where 1=1
	<if test="name!=null and name!=''">
      AND b.name=#{name,jdbcType=VARCHAR}
    </if>
    LIMIT #{sart},#{pageSize}
  </select>

  <select id="countselectAll"  resultType="java.lang.Integer">
    SELECT COUNT(*)
    FROM `bookingcommission` b INNER JOIN `room` r ON b.`roomId`=r.`id`
    INNER JOIN `supplier` s ON b.`supplierId`=s.id
    where 1=1
    <if test="name!=null and name!=''">
      AND b.name=#{name,jdbcType=VARCHAR}
    </if>
  </select>
</mapper>